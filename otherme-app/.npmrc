# Local dev workaround: corporate/Avast TLS interception presents a cert Node
# can't verify (UNABLE_TO_VERIFY_LEAF_SIGNATURE), making installs retry for
# minutes per package. Scoped to this project only. Remove on a clean network,
# or replace with `cafile=<corporate-CA.pem>`.
strict-ssl=false
fetch-retries=2
