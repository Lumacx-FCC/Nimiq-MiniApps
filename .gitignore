
# External reference clones (not our code)
reference/

# Office lock files
~$*

# Secrets — never commit env files anywhere in the repo
**/.env.local
**/.env.*.local

# Machine-specific exported CA (otherme-app dev TLS workaround)
**/local-tls-ca.pem

# Redundant extraction of the committed AeternumRoleplay-source.zip
App - Other Me/Apps integration/aeternum-extracted/

# Marketing assets (kept out of the repo)
marketing/

# Dependencies/builds
node_modules/
dist/
